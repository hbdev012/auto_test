require 'spec_helper'

describe 'Factories' do
  # FactoryGirl.registry.each do |r|
  #   next unless r[1].is_a?(FactoryGirl::Factory)
  # 
  #   it "#{r[0]} should be valid" do
  #     3.times { Factory.create(r[0]).should be_valid }
  #   end
  # end
end
