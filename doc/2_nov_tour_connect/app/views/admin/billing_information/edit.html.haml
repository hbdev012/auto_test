%h1 #{current_user.first_name}, let's complete your signup process.

= render 'admin/signup/navigation'

%h2 Step Two: Billing Information

%fieldset
  %legend Bank Details
  = form_for current_company, :as => :company, :url => admin_billing_information_path do |form|
    = render 'shared/error_messages', { :targets => [form.object] }

    = render :partial => 'billing_information_form', :locals => { :form => form }

    = form.submit 'Save & Next', :disable_with => 'Submitting...'