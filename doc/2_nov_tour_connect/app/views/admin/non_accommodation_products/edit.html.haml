%h1 Add new property/location Non-Accommodation product

%div.sidebar
  = render :partial => 'non_accommodation_product_nav'

%div.content
  %p= "Last Save: #{distance_of_time_in_words_to_now(non_accommodation_product.updated_at, false)} ago"

  = render :partial => params[:step]
